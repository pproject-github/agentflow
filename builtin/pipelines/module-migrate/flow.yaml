# 这个文件只剩两个作用：让流程目录被 catalog 认出来，以及给列表提供 ui.description。
# 图结构在 workspace.flow.js 里，Start/End Pipeline 执行栈已经退休。
instances: {}
edges: []
ui:
  nodePositions: {}
  description: 模块迁移（定范围 → 人工确认 → 建模块 → 迁移 → 静态检查 → 编译修复 → 出文档）
