// Space is still an internal demo. Keep its routes and data intact while hiding
// product entry points until the experience is ready to ship.
export const SPACES_UI_ENABLED = false;

// Feedback currently has no product usage. Preserve the page and stored entries
// while removing its navigation entry.
export const FEEDBACK_UI_ENABLED = false;
